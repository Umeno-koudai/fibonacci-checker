// 文字列を受け取ってコンソールに出力する関数
function printText(text) {
    console.log(text);
}

// 使用例
printText("Hello, World!");

// 2つの数値を受け取って足し算する関数
function addNumbers(num1, num2) {
    return num1 + num2;
}

// 使用例
const result = addNumbers(5, 3);
console.log(result); // 8が出力されます